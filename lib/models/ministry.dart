import 'package:habari_njema/models/contact.dart';

class Ministry {
  String title, description, imageUrl;
  List<Contact> contactDetails;

  Ministry({this.title, this.description, this.contactDetails, this.imageUrl});
}
